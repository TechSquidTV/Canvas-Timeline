---
---

Tighten the renderer theme contract to resolve only documented timeline tokens,
documented shadcn semantic tokens, and built-in defaults. Undocumented aliases
such as `--timeline-focus-ring`, `--timeline-font-mono`, and
`--timeline-font-sans` are no longer read.
